<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_main"
    android:padding="24dp">

    <!-- Header avec logo et menu -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp">

        <TextView
            android:id="@+id/logo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="JNB FITNESS"
            style="@style/TextAppearance.App.H3"
            android:textColor="@color/orange_strong"
            android:letterSpacing="0.05" />

        <!-- Icône menu simplifiée -->
        <TextView
            android:id="@+id/ic_menu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="☰"
            android:textColor="@color/white"
            android:textSize="24sp"
            android:gravity="center"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <!-- Contenu principal -->
    <LinearLayout
        android:id="@+id/main_content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Titre principal -->
        <TextView
            android:id="@+id/hero_title1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Transformez Votre Corps,"
            style="@style/TextAppearance.App.H1"
            android:textColor="@color/white" />

        <TextView
            android:id="@+id/hero_title2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Transformez Votre Vie"
            style="@style/TextAppearance.App.H1"
            android:textColor="@color/orange_strong"
            android:layout_marginTop="4dp" />

        <!-- Description -->
        <TextView
            android:id="@+id/hero_desc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Rejoignez la meilleure salle de sport avec des équipements modernes, des coachs experts et une communauté motivée."
            style="@style/TextAppearance.App.Body"
            android:layout_marginTop="24dp" />

        <!-- Boutons avec design moderne -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="48dp">

            <!-- Bouton principal -->
            <Button
                android:id="@+id/btn_start"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Commencer Maintenant"
                style="@style/Widget.App.Button.Gradient"
                android:layout_marginBottom="16dp" />

            <!-- Bouton secondaire -->
            <Button
                android:id="@+id/btn_login"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Se Connecter"
                style="@style/Widget.App.Button.Outline" />
        </LinearLayout>

        <!-- Indicateur de défilement -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="64dp"
            android:layout_gravity="center">

            <View
                android:layout_width="24dp"
                android:layout_height="6dp"
                android:background="@drawable/gradient_brown_orange"
                android:layout_marginEnd="8dp" />

            <View
                android:layout_width="8dp"
                android:layout_height="6dp"
                android:background="@color/surface_variant"
                android:layout_marginEnd="8dp" />

            <View
                android:layout_width="8dp"
                android:layout_height="6dp"
                android:background="@color/surface_variant" />
        </LinearLayout>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>